version: '3.8'

services:
  metallurgy-analyzer:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: metallurgy-analyzer
    ports:
      - "8501:8501"
    volumes:
      # Монтирование папки data для доступа к файлам
      - ./data:/app/data:ro
      # Монтирование папки для экспорта результатов
      - ./exports:/app/exports
    environment:
      - STREAMLIT_SERVER_HEADLESS=true
      - STREAMLIT_SERVER_ENABLE_CORS=false
      - STREAMLIT_SERVER_ENABLE_XSRF_PROTECTION=false
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8501/_stcore/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s